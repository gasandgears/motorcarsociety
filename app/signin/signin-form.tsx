"use client";
import { useState } from "react";
import { createClient } from "@/lib/supabase/client";
export default function SignInForm({ returnTo }: { returnTo: string }) { const [email, setEmail] = useState(""); const [message, setMessage] = useState(""); const [sending, setSending] = useState(false); const submit = async (event: React.FormEvent) => { event.preventDefault(); setSending(true); setMessage(""); const { error } = await createClient().auth.signInWithOtp({ email, options: { emailRedirectTo: `${window.location.origin}/auth/callback?next=${encodeURIComponent(returnTo)}` } }); setMessage(error ? error.message : "Check your email for a secure sign-in link."); setSending(false); }; return <main className="grid min-h-screen place-items-center bg-[#101211] px-5 py-12 text-white"><section className="w-full max-w-md rounded-2xl border border-white/10 bg-[#181a19] p-7 sm:p-9"><p className="eyebrow">Motorcar Society</p><h1 className="mt-4 font-display text-4xl">Private Registry sign in</h1><p className="mt-4 leading-7 text-white/58">Enter your approved email address. We’ll send a secure sign-in link—no password or ChatGPT account required.</p><form onSubmit={submit} className="mt-7"><label className="field-label" htmlFor="signin-email">Email address</label><input id="signin-email" type="email" required value={email} onChange={(event) => setEmail(event.target.value)} className="field mt-2" placeholder="you@example.com" /><button disabled={sending} className="mt-5 min-h-14 w-full rounded-lg bg-[var(--gold)] px-6 font-semibold text-[#111] disabled:opacity-50">{sending ? "Sending…" : "Email me a sign-in link"}</button></form>{message && <p className="mt-5 rounded-lg border border-white/10 bg-white/5 p-4 text-sm leading-6">{message}</p>}</section></main>; }
